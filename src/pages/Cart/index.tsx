import { Layout } from "../../components/Layout"
import { TableCart } from "../../components/TableCart"

export const Cart = ()=>{
    return (
        <Layout>
          <TableCart />
        </Layout>
    )
}